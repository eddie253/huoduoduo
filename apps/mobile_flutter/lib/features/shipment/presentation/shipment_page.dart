import 'package:flutter/material.dart';

class ShipmentPage extends StatelessWidget {
  const ShipmentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Shipment feature placeholder')
      )
    );
  }
}
